---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { useTranslations } from '../../i18n/utils';
const locale = 'id';
const t = useTranslations(locale);
const categories = [
  { name: t.products.skincare, desc: t.products.skincareDesc, emoji: '✨', items: ['Serum Wajah', 'Pelembab', 'Pembersih', 'Toner', 'Krim Mata', 'Masker Wajah'] },
  { name: t.products.haircare, desc: t.products.haircareDesc, emoji: '💇', items: ['Sampo', 'Kondisioner', 'Masker Rambut', 'Minyak Rambut', 'Gel Styling', 'Semprotan Rambut'] },
  { name: t.products.bodycare, desc: t.products.bodycareDesc, emoji: '🧴', items: ['Lotion Tubuh', 'Scrub Tubuh', 'Body Butter', 'Krim Tangan', 'Sabun Mandi', 'Shower Gel'] },
  { name: t.products.makeup, desc: t.products.makeupDesc, emoji: '💄', items: ['Foundation', 'Lipstik', 'Eyeshadow', 'Blush On', 'Mascara', 'Setting Spray'] },
  { name: t.products.sunscreen, desc: t.products.sunscreenDesc, emoji: '☀️', items: ['Krim Sinar', 'Semprotan Sinar', 'Gel After-Sun', 'BB Cream SPF', 'Lip Balm SPF', 'Sun Stick'] },
  { name: t.products.personal, desc: t.products.personalDesc, emoji: '🧼', items: ['Deodoran', 'Hand Sanitizer', 'Tisu Basah', 'Sabun Tangan', 'Krim Kaki', 'Lip Balm'] },
];
---

<Layout title={`${t.nav.products} - CosmoCare`} description={t.products.subtitle} locale={locale}>
  <Header {t} {locale} />
  <main class="pt-20">
    <section class="py-16 md:py-24">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
          <span class="inline-block px-4 py-1.5 bg-primary-100 text-primary-700 text-sm font-medium rounded-full mb-4">{t.nav.products}</span>
          <h1 class="section-title">{t.products.title}</h1>
          <p class="section-subtitle">{t.products.subtitle}</p>
        </div>
        <div class="space-y-12">
          {categories.map((cat) => (
            <div class="bg-white rounded-2xl border border-gray-100 p-8 hover:shadow-md transition-shadow">
              <div class="flex items-start gap-6">
                <div class="text-4xl">{cat.emoji}</div>
                <div class="flex-1">
                  <h2 class="text-2xl font-semibold text-brand-dark mb-2">{cat.name}</h2>
                  <p class="text-gray-600 mb-4">{cat.desc}</p>
                  <div class="grid grid-cols-2 md:grid-cols-3 gap-2">
                    {cat.items.map((item) => (<span class="inline-flex items-center px-3 py-1.5 bg-brand-cream text-gray-700 text-sm rounded-lg">{item}</span>))}
                  </div>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
    <section class="py-16 md:py-24 bg-brand-cream">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-3xl font-serif font-bold text-brand-dark mb-6">{t.products.oem}</h2>
        <p class="text-gray-600 text-lg mb-8">{t.products.oemDesc}</p>
        <a href={`/${locale}/services`} class="btn-primary text-lg px-8 py-4">{t.hero.learnMore}</a>
      </div>
    </section>
  </main>
  <Footer {t} {locale} />
</Layout>
